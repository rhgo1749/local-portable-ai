import type { PDFiumLibrary } from "../library.js";
import type { RPCRequest } from "./protocol.js";
export type DispatchResult = {
    value: unknown;
    transfer: Transferable[];
};
export type LibraryFactory = () => Promise<PDFiumLibrary>;
export declare class WorkerDispatcher {
    private library;
    private initPromise;
    private nextHandleId;
    private readonly documents;
    private readonly pages;
    private readonly pagesByDoc;
    private readonly factory;
    constructor(factory: LibraryFactory);
    private makeHandle;
    private ensureLibrary;
    private getDoc;
    private getPage;
    private disposeDocument;
    dispatch(req: RPCRequest): Promise<DispatchResult>;
    liveDocumentCount(): number;
    livePageCount(): number;
}
